let nome: string = 'João'
// if assign nome = 123 will return error
console.log(nome)

let estaCovendo: boolean
// if we use variable before assign, like console.log(estaCovendo), will return error 
estaCovendo = true
console.log(estaCovendo)

let idade = 27
// if we try assign a string, like idade = 'abc' return error: Type 'string' is not assignable to type 'number'. 
